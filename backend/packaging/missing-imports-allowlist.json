{
  "schemaVersion": 2,
  "target": {
    "os": "darwin",
    "architecture": "arm64",
    "pythonVersion": "3.13.14"
  },
  "pyinstallerVersion": "6.21.0",
  "pyinstallerHooksContribVersion": "2026.6",
  "modules": {
    "_frozen_importlib_external": "CPython bootstrap module referenced by importlib analysis",
    "_manylinux": "Setuptools packaging probe for Linux wheel tags; target is Darwin arm64",
    "_overlapped": "Windows-only compatibility or console/event-loop path",
    "_pytest": "Optional test-framework integration; absent from the production lock",
    "_pytest.outcomes": "Optional test-framework integration; absent from the production lock",
    "_typeshed": "Typing or development-only probe; absent from the production lock",
    "_winapi": "Windows-only compatibility or console/event-loop path",
    "_wmi": "Windows-only compatibility or console/event-loop path",
    "a2wsgi": "Optional Uvicorn deployment adapter; sidecar uses the reviewed native UDS runner",
    "annotationlib": "Python-version conditional annotation helper; absent from CPython 3.13",
    "asyncio.DefaultEventLoopPolicy": "PyInstaller attribute-as-submodule false positive; asyncio is bundled",
    "brotli": "Optional HTTP content-coding backend; absent from the production lock",
    "brotlicffi": "Optional HTTP content-coding backend; absent from the production lock",
    "cdifflib": "Optional Dulwich accelerator or merge helper; absent from the production lock",
    "collections.abc": "CPython 3.13 virtual alias backed by _collections_abc, not a physical submodule",
    "colorama": "Windows-only compatibility or console/event-loop path",
    "compression": "Python-version conditional compression package; absent from CPython 3.13",
    "cython": "Typing or development-only probe; absent from the production lock",
    "dissolve": "Optional Dulwich accelerator or merge helper; absent from the production lock",
    "dotenv": "Optional Uvicorn configuration loader; sidecar passes reviewed settings directly",
    "email_validator": "Optional Pydantic field-type dependency not used by sidecar schemas",
    "eval_type_backport": "Optional Pydantic typing-evaluation fallback; absent from the production lock",
    "exceptiongroup": "Compatibility backport not required by the reviewed CPython 3.13 target",
    "gpg": "Optional Dulwich signature backend; absent from the production lock",
    "gunicorn": "Optional Uvicorn deployment adapter; sidecar uses the reviewed native UDS runner",
    "gunicorn.workers": "Optional Uvicorn deployment adapter; sidecar uses the reviewed native UDS runner",
    "h2": "Optional urllib3 HTTP/2 backend; absent from the production lock",
    "h2.connection": "Optional urllib3 HTTP/2 backend; absent from the production lock",
    "h2.events": "Optional urllib3 HTTP/2 backend; absent from the production lock",
    "hypothesis": "Optional test-framework integration; absent from the production lock",
    "importlib_resources": "Optional importlib backport; not required by CPython 3.13",
    "itsdangerous": "Optional Starlette session middleware; not configured by the sidecar",
    "itsdangerous.exc": "Optional Starlette session middleware; not configured by the sidecar",
    "java": "Jython-only platform compatibility probe",
    "java.lang": "Jython-only platform compatibility probe",
    "js": "Pyodide/Emscripten-only compatibility path",
    "merge3": "Optional Dulwich accelerator or merge helper; absent from the production lock",
    "msvcrt": "Windows-only compatibility or console/event-loop path",
    "multipart": "Optional Starlette/FastAPI form parser; the sidecar API is JSON-only",
    "multipart.multipart": "Optional Starlette/FastAPI form parser; the sidecar API is JSON-only",
    "multiprocessing.AuthenticationError": "PyInstaller attribute-as-submodule false positive; multiprocessing is bundled",
    "multiprocessing.BufferTooShort": "PyInstaller attribute-as-submodule false positive; multiprocessing is bundled",
    "multiprocessing.get_context": "PyInstaller attribute-as-submodule false positive; multiprocessing is bundled",
    "multiprocessing.get_start_method": "PyInstaller attribute-as-submodule false positive; multiprocessing is bundled",
    "multiprocessing.Pipe": "PyInstaller attribute-as-submodule false positive; multiprocessing is bundled",
    "multiprocessing.set_start_method": "PyInstaller attribute-as-submodule false positive; multiprocessing is bundled",
    "multiprocessing.TimeoutError": "PyInstaller attribute-as-submodule false positive; multiprocessing is bundled",
    "mypy": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.errorcodes": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.expandtype": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.nodes": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.options": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.plugin": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.plugins": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.semanal": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.server": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.state": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.type_visitor": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.typeops": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.types": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.typevars": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.util": "Pydantic static-type-checker plugin path; absent from the production lock",
    "mypy.version": "Pydantic static-type-checker plugin path; absent from the production lock",
    "nt": "Windows-only compatibility or console/event-loop path",
    "outcome": "Optional AnyIO Trio backend; sidecar uses asyncio",
    "pathlib.types": "Python-version conditional typing helper; absent from CPython 3.13",
    "patiencediff": "Optional Dulwich accelerator or merge helper; absent from the production lock",
    "pydantic_extra_types": "Optional Pydantic field-type dependency not used by sidecar schemas",
    "pydantic.BaseModel": "PyInstaller attribute-as-submodule false positive; the Pydantic export is bundled",
    "pydantic.PydanticSchemaGenerationError": "PyInstaller attribute-as-submodule false positive; the Pydantic export is bundled",
    "pydantic.PydanticUserError": "PyInstaller attribute-as-submodule false positive; the Pydantic export is bundled",
    "pyimod02_importers": "PyInstaller runtime importer supplied by the frozen bootloader",
    "pyodide": "Pyodide/Emscripten-only compatibility path",
    "pyodide.ffi": "Pyodide/Emscripten-only compatibility path",
    "python_multipart": "Optional Starlette/FastAPI form parser; the sidecar API is JSON-only",
    "python_multipart.multipart": "Optional Starlette/FastAPI form parser; the sidecar API is JSON-only",
    "pywintypes": "Windows-only compatibility or console/event-loop path",
    "rich": "Optional Pydantic pretty-print integration; absent from the production lock",
    "rich.pretty": "Optional Pydantic pretty-print integration; absent from the production lock",
    "setuptools._vendor.backports.zstd": "Optional HTTP content-coding backend; absent from the production lock",
    "sitecustomize": "Optional interpreter startup customization intentionally disabled by isolated mode",
    "sniffio": "Optional AnyIO legacy async detector; sidecar uses asyncio",
    "sshsig": "Optional Dulwich signature backend; absent from the production lock",
    "sshsig.allowed_signers": "Optional Dulwich signature backend; absent from the production lock",
    "sshsig.ssh_public_key": "Optional Dulwich signature backend; absent from the production lock",
    "sshsig.sshsig": "Optional Dulwich signature backend; absent from the production lock",
    "toml": "Pydantic static-type-checker plugin path; absent from the production lock",
    "trio": "Optional AnyIO Trio backend; sidecar uses asyncio",
    "trio.lowlevel": "Optional AnyIO Trio backend; sidecar uses asyncio",
    "trio.socket": "Optional AnyIO Trio backend; sidecar uses asyncio",
    "trio.testing": "Optional AnyIO Trio backend; sidecar uses asyncio",
    "trio.to_thread": "Optional AnyIO Trio backend; sidecar uses asyncio",
    "trove_classifiers": "Optional Setuptools metadata validation helper; absent at runtime",
    "usercustomize": "Optional interpreter startup customization intentionally disabled by isolated mode",
    "uvicorn.protocols.websockets.websockets_impl": "WebSocket transports are intentionally excluded by the sidecar spec and protocol",
    "uvicorn.protocols.websockets.websockets_sansio_impl": "WebSocket transports are intentionally excluded by the sidecar spec and protocol",
    "uvicorn.protocols.websockets.wsproto_impl": "WebSocket transports are intentionally excluded by the sidecar spec and protocol",
    "vms_lib": "OpenVMS-only platform probe",
    "win32api": "Windows-only compatibility or console/event-loop path",
    "win32evtlog": "Windows-only compatibility or console/event-loop path",
    "win32evtlogutil": "Windows-only compatibility or console/event-loop path",
    "winloop": "Windows-only compatibility or console/event-loop path",
    "winreg": "Windows-only compatibility or console/event-loop path",
    "yaml": "Optional Uvicorn configuration loader; sidecar passes reviewed settings directly"
  }
}
