---
gate_id: VAL-LEGACY-SCOPE-001
status: pass
recorded_at: 2026-07-31T11:24:08Z

repository: fredgnr/local-context-forge
branch: agent/documentation-handoff
source_commit: 64ec3c232d08f1e843d81dc7c4972dc5ebf96c9b
source_tree: 89190ff4296a34d690a0f3a3eda1252463323d8f
tag: null
workflow_file_commit: null
actions_run_url: null
release_id: null

product_version: 0.3.0-alpha.1
database_schema: 5
architecture: x86_64
machine_model: hosted Linux workspace
macos_build: null
runtime_versions:
  python: 3.12.13
  node: 24.14.0
  npm: 11.9.0
  ripgrep: 15.2.0

candidate_manifest_sha256: null
credential_generation_id: null
certificate_sha256: null
update_public_key_sha256: null
asset_sha256s: {}

executor: Codex primary documentation agent
independent_reviewer:
  - architecture, deletion-gate and release-order audit
  - path ownership, TODO cardinality and active-authority audit
started_at: 2026-07-31T10:35:00Z
completed_at: 2026-07-31T11:24:08Z

preconditions:
  - clean detached checkout at the exact source commit
  - planning-only documentation scope from ITER-0002/R12
  - no runtime/source/workflow deletion or external asset operation
commands:
  - python3 -B tools/check_markdown_links.py
  - python3 /root/.codex/skills/oai/skill-creator/scripts/quick_validate.py .agents/skills/lcf-desktop-development
  - python3 -B tools/check_version_sync.py
  - git diff --check
  - exact TODO summary/traceability ID and status-count check
  - changed-path Markdown-only and guide-site exclusion check
  - changed-file Markdown fence parity check
expected_results:
  - all relative Markdown links resolve
  - the project desktop skill remains structurally valid
  - product, protocol and schema versions remain synchronized
  - no whitespace error exists
  - exactly 36 stable task IDs match between TODO and traceability
  - task states are exactly 28 planned, 5 blocked and 3 superseded
  - all 52 content-checkpoint paths are Markdown inside the declared R12 scope
  - every changed Markdown file has balanced fences
  - remove, retain and split ownership plus the global destructive gate are unambiguous
actual_results:
  - Markdown link validation passed for 81 files
  - desktop skill validation passed
  - version synchronization passed for 0.3.0-alpha.1 and desktop protocol 1.0
  - git diff check exited 0
  - TODO and traceability contained the same 36 exact IDs
  - status counts were 28 planned, 5 blocked and 3 superseded
  - changed_paths=52, markdown_only=true, guide_site_changed=false
  - Markdown fence parity passed for all 52 changed files
  - final independent audit found no High or Medium blocker for the declared R12 scope
failure_injections: []
data_before_counts: {}
data_after_counts: {}
rollback_result: not-run
sanitized_artifacts:
  - this repository record
known_limitations:
  - GitHub Actions had not run for this branch checkpoint when recorded
  - the evidence/status follow-up commit and final PR head contain additional evidence metadata
  - guide-site remains stale and cannot be edited safely until its exact Sites identity is recovered
  - no runtime implementation, code deletion, packaged M4, provider, model, MCP, signing, release, update or absence gate ran
  - VAL-ELECTRON-CUTOVER-001 and VAL-LEGACY-ABSENCE-001 remain not-run
redactions_confirmed:
  - no credential, auth cache, private source, user data, model, index, certificate or private path recorded
---

# VAL-LEGACY-SCOPE-001：Electron-only 退役范围检查点

## Summary

提交 `64ec3c232d08f1e843d81dc7c4972dc5ebf96c9b` / tree
`89190ff4296a34d690a0f3a3eda1252463323d8f` 冻结了 ADR-0015、严格
`remove` / `retain` / `split` manifest、36 项 TODO roll-up、ITER-0007 和对应权威文档。

本记录只把 **planning/documentation scope** 标为 `pass`：每个 legacy capability 有 owner、
Electron replacement、全局删除门禁和最终 absence gate；`web/src/**`、`backend/app/**`、
Desktop MCP/QMD/release 等共享或受保护 surface 没有被误列为整目录删除。它不表示任何
legacy 文件已删除，也不提升 packaged/physical/release gate。

## Scope proof

内容检查点修改 52 个 Markdown 文件，全部落在 R12 明确列出的治理、索引、ADR metadata、
component guidance 和 development records 中。它没有修改 `desktop/**`、`backend/app/**`、
`web/src/**`、`mcp/**`、`host_runner/**`、`docker/**`、`scripts/**`、`runtime/**`、
`guide-site/**`、Makefile、product/release workflow、dependency/lock 或 generated artifact。

未来代码删除不继承本提交的写权限。每个实现 PR 仍须重新声明 exact paths，并遵循：

```text
inventory → split without deletion → non-publishing packaged M4 cutover
→ remove → docs/release cleanup → final-bytes packaged rerun → absence gate
```

任何一个 capability 未替代都会阻断全部 destructive removal。Pre-1.0 不提供 legacy API、
config、data 或 deployment compatibility；这不授权静默删除 data、volume、backup、package、
Release 或任何其他外部资产。

## Review

独立只读复核重点检查了：

1. `web` renderer、public Web、private UDS sidecar、public API、legacy MCP 和 desktop MCP 的
   名称/owner 是否被混淆；
2. `web/package.json`、`backend/app/cli.py`、`desktop_generation.py`、runtime contracts、
   handbook builder 和 active docs 等 mixed surface 是否被正确标为 `split`；
3. packaged cutover、container workflow deletion、final-byte rerun、absence gate 和正式
   Draft/promotion 是否形成循环依赖；
4. 36 个任务、3 个 superseded tombstone、ADR partial-supersession metadata 与唯一 MCP task
   owner 是否一致；
5. 不兼容政策是否始终与“不自动删除用户/远端资产”分离。

修复后，复核没有发现 R12 planning-only scope 的 High/Medium 阻塞。公开 `guide-site` 仍是
E06、`VAL-LEGACY-ABSENCE-001` 和 Electron-only Release 的 High blocker；它已被标为
non-authoritative，并因当前 checkout 缺失精确 Sites identity 而 fail closed，不冒充本轮完成。

## Exact validation

检查在 detached、clean checkout 上执行。核心结果：

```text
Markdown link validation passed: 81 files
Skill is valid!
Version synchronization passed: 0.3.0-alpha.1, desktop protocol 1.0
summary_ids=36 summary_rows=36 trace_exact_ids=36
sets_equal=true status_counts={planned: 28, blocked: 5, superseded: 3}
changed_paths=52 markdown_only=true guide_site_changed=false
markdown_fence_parity_files=52 failures=[]
```

## Release disposition

**Source merge GO / public release NO-GO.**

`VAL-LEGACY-SCOPE-001=pass` 只允许后续按顺序完成 capability closure 和解耦。真正删除仍要求
`VAL-ELECTRON-CUTOVER-001=pass`；最终收敛还要求 removal commit 的新 candidate digest 重跑
完整核心 M4 matrix，并取得 `VAL-LEGACY-ABSENCE-001=pass`。本记录没有创建 tag、Draft、
Release、DMG、container image、签名、GitHub settings 或公开 Sites deployment。
