# W02 engineering-smoke 静态 assembly 证据

本记录追加在 [W02 entry](2026-08-05-entry.md) 之后，只证明 Draft PR #21 exact head 的
macOS arm64 engineering-smoke `.app` directory 已完成静态 assembly 与 bundle audit。该 job
没有启动 assembled `.app`，也没有从 bundle 启动 sidecar 或运行 packaged launch/runtime
smoke；因此它不把
`VAL-PACKAGED-SMOKE-001` 提升为 `pass`，不解锁 W10/W11，也不改变 public release
`NO-GO`。

## 权威坐标

| 字段 | 值 |
| --- | --- |
| recorded at | `2026-08-06` |
| canonical repository | `fredgnr/local-context-forge` |
| PR | [Draft #21](https://github.com/fredgnr/local-context-forge/pull/21) |
| branch | `agent/w02a-engineering-smoke-boundary` |
| exact source commit | `08137c7bce5469350b861cef7960e4a0530151bf` |
| exact source tree | `d7814ac96136cea33fb7069d9538a4aad8dffa38` |
| source date epoch | `1785946811` (`2026-08-05T16:20:11Z`) |
| workflow | `.github/workflows/packaged-smoke.yml` at the exact source commit |
| assembly run / job | [Engineering smoke bundle assembly 31024794972](https://github.com/fredgnr/local-context-forge/actions/runs/31024794972) / [job 92370351806](https://github.com/fredgnr/local-context-forge/actions/runs/31024794972/job/92370351806) |
| assembly job conclusion | `success`；all steps `success` |
| exact-head source run | [Desktop source CI 31024794734](https://github.com/fredgnr/local-context-forge/actions/runs/31024794734) / `success` |
| remote artifacts | empty |

<!-- w02-assembly-authority: source=08137c7bce5469350b861cef7960e4a0530151bf,tree=d7814ac96136cea33fb7069d9538a4aad8dffa38,assembly-run=31024794972,assembly-job=92370351806,source-run=31024794734,inventory-sha256=7fcdb699ad367e7c7da28a074694c6fe8a0a67b54829173894d311de4f6ffe5c,inventory-entries=879,native-files=78,source-date-epoch=1785946811 -->

## 环境与过程

- GitHub-hosted `macos-15` native arm64 runner；workflow 在运行前验证 exact commit、clean
  checkout 和 `uname -m=arm64`；
- locked Node `22.23.2`、reviewed build Python `3.13.14`、Electron `43.2.0`；
- assembly workflow 依次执行 policy check、locked Python sidecar build/audit、renderer
  build/stage、Desktop typecheck/tests/build，以及以下静态 assembly/audit 命令：

```bash
npm --prefix desktop run prepare:engineering-smoke
CSC_FOR_PULL_REQUEST=true npm --prefix desktop run pack:engineering-smoke
npm --prefix desktop --silent run audit:engineering-smoke
```

job 的每个 step 均为 `success`。其中 pre-pack frozen sidecar staging smoke 已运行且成功；
它由 `make python-sidecar-build` 执行，只是 sidecar build/staging evidence，不是从 assembled
`.app` 启动的 packaged runtime smoke。独立的 exact-head Desktop source CI run
`31024794734` 也为 `success`。

## 静态 assembly 结果

| 观察 | 结果 |
| --- | --- |
| distribution | `engineering-smoke` / `UNOFFICIAL` / `engineeringOnly=true` / `publishable=false` |
| target | macOS arm64 `app-dir`；不是 DMG、ZIP、安装或发行候选 |
| app ID | `dev.localcontextforge.engineering-smoke` |
| product name | `Local Context Forge Engineering Smoke` |
| normalized inventory SHA-256 | `7fcdb699ad367e7c7da28a074694c6fe8a0a67b54829173894d311de4f6ffe5c` |
| inventory entries | `879` |
| native files | `78` |
| `assembly` | `pass` |
| `bundleAudit` | `pass` |
| electron-builder identity | `identityName=-` / `identityHash=none`（ad-hoc engineering identity） |
| notarization | `false` |

远端 artifact 列表为空；本记录没有可下载的 `.app` archive 或 package digest。上述 SHA-256
只绑定该 run 的规范化 inventory，不得冒充 retained package/blob digest，也不能让后续 launch
复用未经重新绑定的 bytes。

## 明确未运行与限制

- assembled packaged `.app` 未启动，sidecar 未从 assembled bundle 启动；pre-pack frozen
  sidecar staging smoke 已运行且成功，但只属于 staging evidence；
- assembled bundle 的 renderer/preload handshake、Main → private UDS health/domain request、
  quit/no orphan、public INET listener observation 和 packaged exercised-path system
  Python/Node/Git PATH trap 均未运行；
- `packagedAppLaunch`：`not-run`；
- `packagedSmokeValidation`：`not-run`；
- `VAL-PACKAGED-SMOKE-001`：`not-run`；
- W10/W11：`locked`；
- production signing、assembled `.app` Gatekeeper/`spctl` assessment、tag、artifact upload、
  Draft/Release、promotion 与 update：`not-run`；notarization config/result 为 `false`；
- rollback：`not-run`；job 只生成 runner 临时目录内容，且没有远端 artifact；
- independent review / resulting-main packaged rerun：`not-run`；PR #21 仍是 Draft；
- public release：`NO-GO`。

结论：exact source commit 的 **static assembly/bundle audit substage `pass`**；packaged
launch/runtime gate 仍为 `not-run`。未来 Work 必须构建并绑定 fresh exact bytes、实际启动 App，
完成全部 runtime 观察和独立验收/resulting-main 重跑后，才可单独评估
`VAL-PACKAGED-SMOKE-001`。
