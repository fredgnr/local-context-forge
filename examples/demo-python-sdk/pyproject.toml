[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "lcf-demo-sdk"
version = "1.0.0"
description = "Deterministic example repository for Local Context Forge"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
